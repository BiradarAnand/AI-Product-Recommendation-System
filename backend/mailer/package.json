{
  "name": "recovibe-mailer",
  "version": "1.0.0",
  "description": "Nodemailer OTP sender for RecoVibe — called as subprocess from Flask",
  "main": "send_otp.js",
  "scripts": {
    "test-otp": "node send_otp.js --name \"Test User\" --email \"test@example.com\" --otp \"123456\""
  },
  "dependencies": {
    "nodemailer": "^8.0.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
